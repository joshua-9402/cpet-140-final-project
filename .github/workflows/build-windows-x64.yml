name: Build Windows x64 (Manual)

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'Release'
        type: choice
        options:
          - Release
          - Debug
      enable_lto:
        description: 'Enable Link-Time Optimization'
        required: false
        default: true
        type: boolean

jobs:
  build-windows-x64:
    name: Windows x64
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: System information
      shell: pwsh
      run: |
        Write-Host "=== System Info ==="
        Get-ComputerInfo | Select-Object WindowsProductName, WindowsVersion, OsArchitecture
        Write-Host "`n=== CPU Info ==="
        Get-WmiObject Win32_Processor | Select-Object Name, NumberOfCores, NumberOfLogicalProcessors
        Write-Host "`n=== Available Memory ==="
        $memory = Get-WmiObject Win32_OperatingSystem
        Write-Host "Total RAM: $([math]::Round($memory.TotalVisibleMemorySize/1MB, 2)) GB"

    - name: Install Ninja
      shell: pwsh
      run: |
        Write-Host "=== Installing Ninja build system ==="
        choco install ninja -y
        ninja --version

    - name: Setup MSVC
      uses: microsoft/setup-msbuild@v2

    - name: Configure CMake
      shell: pwsh
      run: |
        Write-Host "=== Configuring CMake ==="
        if ("${{ inputs.enable_lto }}" -eq "true") {
          cmake -B build `
            -DCMAKE_BUILD_TYPE=${{ inputs.build_type }} `
            -G Ninja `
            -DCMAKE_C_COMPILER=cl `
            -DCMAKE_CXX_COMPILER=cl
        } else {
          cmake -B build `
            -DCMAKE_BUILD_TYPE=${{ inputs.build_type }} `
            -G Ninja `
            -DCMAKE_C_COMPILER=cl `
            -DCMAKE_CXX_COMPILER=cl `
            -DNO_LTO=ON
        }

    - name: Build
      shell: pwsh
      run: |
        Write-Host "=== Building application ==="
        $cores = (Get-WmiObject Win32_Processor).NumberOfLogicalProcessors
        cmake --build build --config ${{ inputs.build_type }} -j $cores

    - name: Verify build output
      shell: pwsh
      run: |
        Write-Host "=== Checking build output ==="
        Get-ChildItem -Recurse build | Where-Object {$_.Name -like "*payroll*"} | Select-Object FullName, Length
        
        $exe = Get-ChildItem -Recurse build -Filter "payroll-and-monitoring-system.exe" | Select-Object -First 1
        
        if ($exe) {
          Write-Host "`n✅ Executable found: $($exe.FullName)"
          Write-Host "Size: $([math]::Round($exe.Length/1MB, 2)) MB"
          
          # Verify it's x64
          $fileInfo = & dumpbin /headers $exe.FullName | Select-String "machine"
          Write-Host "`n=== Architecture Verification ==="
          Write-Host $fileInfo
        } else {
          Write-Host "`n❌ Build output not found!"
          exit 1
        }

    - name: Package artifacts
      shell: pwsh
      run: |
        Write-Host "=== Packaging application ==="
        New-Item -ItemType Directory -Force -Path artifact
        
        # Find and copy executable
        $exe = Get-ChildItem -Recurse build -Filter "payroll-and-monitoring-system.exe" | Select-Object -First 1
        
        if ($exe) {
          Copy-Item $exe.FullName artifact/
          Write-Host "Copied executable: $($exe.Name)"
        } else {
          Write-Error "Executable not found!"
          exit 1
        }
        
        # Copy assets
        if (Test-Path assets) {
          Copy-Item -Recurse assets artifact/
          Write-Host "Copied assets folder"
        }
        
        # Create archive
        $archiveName = "payroll-and-monitoring-system-windows-x64-${{ inputs.build_type }}.zip"
        Compress-Archive -Path artifact/* -DestinationPath $archiveName -CompressionLevel Optimal
        
        Write-Host "`n=== Archive created ==="
        Get-Item $archiveName | Select-Object Name, Length

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: payroll-and-monitoring-system-windows-x64-${{ inputs.build_type }}
        path: payroll-and-monitoring-system-windows-x64-${{ inputs.build_type }}.zip
        retention-days: 90
        compression-level: 0

    - name: Build summary
      shell: pwsh
      run: |
        $summary = "### ✅ Build Completed Successfully`n`n"
        $summary += "Platform: Windows x64`n"
        $summary += "Build Type: ${{ inputs.build_type }}`n"
        $summary += "LTO Enabled: ${{ inputs.enable_lto }}`n"
        $summary += "Compiler: MSVC (Microsoft Visual C++)`n"
        $summary += "Architecture: x86-64 (64-bit)`n`n"
        $summary += "Artifact: payroll-and-monitoring-system-windows-x64-${{ inputs.build_type }}.zip`n`n"
        $summary += "Download the artifact from the workflow run page."
        
        Add-Content -Path $env:GITHUB_STEP_SUMMARY -Value $summary

