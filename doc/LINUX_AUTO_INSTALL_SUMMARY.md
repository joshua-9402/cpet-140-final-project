# Linux Packaging Removed

Linux installer packaging (.deb/.rpm) has been removed from the release pipeline. The auto-install scripts are retained for reference but are not included in released Linux installers.

For guidance on deploying to Linux, see `doc/LINUX_PACKAGING_REMOVED.md`.
